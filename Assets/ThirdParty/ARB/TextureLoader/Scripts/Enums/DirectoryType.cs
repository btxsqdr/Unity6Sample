namespace ARB.TextureLoader
{
    public enum DirectoryType
    {
        /// <summary>
        /// The path to the persistent data directory (Application.persistentDataPath).
        /// </summary>
        Persistent,

        /// <summary>
        /// The path to the temporary data / cache directory (Application.temporaryCachePath).
        /// </summary>
        Temporary
    }
}
